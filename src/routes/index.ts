const AUTH = {
    ROOT: '/auth',
    LOGIN: '/auth/login',
    REGISTER: '/auth/register',
    VERIFY_EMAIL: '/auth/confirm-email',
};

const PRIVATE = {
    ROOT: '/dashboard'
}

export const ROUTES = {
    AUTH,
    PRIVATE
};